#ifndef PRESETS_HPP
#define PRESETS_HPP

/** Version string for this project. */
#define PROJECT_INFO "AutonomousDrivingProject - v0.0.1"

/** Serial connection to rplidar. */
#define RPLIDAR_CONNECTION "/dev/ttyUSB0"

/** Baud rate of rplidar serial connection. */
#define RPLIDAR_BAUD_RATE 115200

#endif /* end of include guard: PRESETS_HPP */
